name: Deploy Flutter Web to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'          # ou a versão que você usa (ex: '3.24.x')
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get
        working-directory: frontend/mobile   # ← Ajuste se pubspec.yaml estiver em outra pasta!

      - name: Build Flutter Web
        run: flutter build web --release --base-href /
        working-directory: frontend/mobile   # ← Pasta com pubspec.yaml e web/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/mobile/build/web    # ← Pasta gerada pelo flutter build web

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
